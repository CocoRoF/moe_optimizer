"""Methods that were proposed, tested, and rejected -- kept as the record.

``depth_atlas``  orthogonal-polynomial chart over layer depth.  Falsified by F6:
                 adjacent-layer dictionaries share no directions (union rank
                 128/128), so there is nothing smooth enough to chart.  Retained
                 so the ablation row in any future table is reproducible.
"""
